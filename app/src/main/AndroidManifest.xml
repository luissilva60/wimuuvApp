<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wimuuvapplication" >
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.PRIORITY_HIGH_ACCURACY" />
    <uses-permission android:name="android.permission.ACTION_LOCATION_SOURCE_SETTINGS" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar" >
        <activity
            android:name=".UI.Org.SpotDetailsOrg"
            android:exported="false" />
        <activity
            android:name=".UI.Org.FeedDetailsOrg"
            android:exported="false" />
        <activity
            android:name=".UI.Org.SettingsOrgActivity"
            android:exported="false" />
        <activity
            android:name=".UI.Student.QRCodeEventActivity"
            android:exported="true" />
        <activity
            android:name=".UI.Student.SpotDetailsActivity"
            android:exported="true" />
        <activity
            android:name=".UI.Student.FeedDetails"
            android:exported="false" />
        <activity
            android:name=".UI.Org.ForgotPasswordOrg"
            android:exported="false" />
        <activity
            android:name=".UI.Org.OrgLoginActivity"
            android:exported="false" />
        <activity
            android:name=".UI.Org.OrgActivity"
            android:exported="false" />
        <activity
            android:name=".UI.Student.EditPerfil"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDVe28Yx5jnxbaE6HyGVdmly60yIS5k2Io" />

        <activity
            android:name=".UI.Student.MainActivity2"
            android:exported="false" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".UI.Student.Settings"
            android:exported="true" />
        <activity
            android:name=".UI.Student.Register"
            android:exported="false" />
        <activity
            android:name=".UI.Student.ForgotPassword"
            android:exported="false" />
        <activity
            android:name=".Login.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Maps Android Key -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>